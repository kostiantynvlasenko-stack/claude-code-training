{
  "name": "claude-code-training",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@8.15.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:seed": "pnpm --filter @training/api db:seed",
    "db:reset": "pnpm --filter @training/api db:reset",
    "prepare": "echo \"husky disabled - uncomment to enable\""
  },
  "devDependencies": {
    "@secretlint/secretlint-rule-preset-recommend": "^8.0.0",
    "cross-env": "^7.0.3",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "secretlint": "^8.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
